import { describe, it, expect } from 'vitest'

// Simple smoke test to ensure Vitest is wired up

describe('Sidebar smoke test', () => {
    it('runs the test harness', () => {
        expect(true).toBe(true)
    })
})
